export interface IFollowAction {
    // 未來呈現用戶列表功能
    followeeId?: string,
    followeeType?: string,
    followeeName?: string,
    followeeSeoName?: string,
    followeeImage?: string,
    id?: string,// userId
    name?: string,
    image?: string,
}